# =============== Generale / OS ===============
.DS_Store           # macOS system files
.env                # Environment variables for both frontend and backend
**.env               # Catch-all for any .env files
.env.* # Potentially other .env variations
.pytest_cache/      # pytest cache directory
.vscode/            # VS Code specific settings (if you don't want to share)
*.code-workspace    # VS Code workspace file
*~                  # Backup files
*.bak               # Backup files

# =============== Node.js / Frontend (Vite/React) ===============
node_modules/       # Node.js dependency directory
dist/               # Frontend build output directory
build/              # Another common build output directory
npm-debug.log* # npm debug logs
yarn-debug.log* # yarn debug logs
yarn-error.log* # yarn error logs
.pnpm-store/       # pnpm package store
*.tgz               # tarballs generated by npm pack
.npm/               # npm cache directory
.vite/              # Vite cache directory
coverage/           # Jest/Vitest coverage reports
storybook-static/   # Storybook build output
.eslintcache        # ESLint cache
.parcel-cache/      # Parcel cache (if used)

# =============== Python / Backend (FastAPI) ===============
**/__pycache__/        # Python compiled bytecode cache
backend/pinecone/__pycache__/  # Pinecone specific cache
backend/__pycache__/  # General Python cache
backend/scalers/    # Directory for scaler files (if used)
*.pyc               # Python compiled bytecode files
.Python             # Older virtualenv directoy
venv/               # Standard Python virtual environment directory
env/                # Another common virtual environment directory
*.env               # More generic env files (redundant if .env is there, but good for safety)
*.tmp               # Temporary files
*.log               # Log files
*.sqlite3           # SQLite databases (if used locally)
.ipynb_checkpoints  # Jupyter notebook checkpoints
.mypy_cache/        # MyPy cache for type checking
.pytest_cache/      # Pytest cache (redundant if already in General, but good to be explicit)
.ruff_cache/        # Ruff linter cache